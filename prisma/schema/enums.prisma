enum ActiveStatus {
  Active
  Blocked
}

enum Role {
  AUTHOR
  USER
  ADMIN
}

enum Poststatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum CommentStatus {
  APPROVED
  REJECT
}
enum SubscriptionStatus {
  ACTIVE
  CANCELED
  EXPIRED
}